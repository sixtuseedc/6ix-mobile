const require("dotenv").config(); const express = require("express"); const messagingRoutes = require("./routes/messaging"); const voiceRoutes = require("./routes/voice"); const paymentRoutes = require("./routes/payments"); const app = express(); app.use(express.json({ limit: "1mb" })); app.get("/", (_req, res) = res.status(200).send("6ix webhook receiver is running."); }); app.use("/webhooks/telnyx/messaging", messagingRoutes); app.use("/webhooks/telnyx/voice", voiceRoutes); app.use("/api/payments", paymentRoutes); const PORT = process.env.PORT 
